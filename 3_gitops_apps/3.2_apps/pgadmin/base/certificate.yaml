apiVersion: cert-manager.io/v1
kind: Certificate

metadata:

  name: pgadmin-cert

  namespace: pgadmin

spec:

  secretName: pgadmin-cert

  dnsNames:

  - pgadmin.homelab.local

  issuerRef:

    name: homelab-clusterissuer

    kind: ClusterIssuer